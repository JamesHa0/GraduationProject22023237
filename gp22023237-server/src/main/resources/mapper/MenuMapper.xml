<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jameshao.gp22023237.mapper.MenuMapper">

    <resultMap id="BaseResultMap" type="com.jameshao.gp22023237.po.Menu">
            <id property="id" column="id" />
            <result property="menusIndex" column="menus_index" />
            <result property="title" column="title" />
            <result property="icon" column="icon" />
            <result property="path" column="path" />
            <result property="parentId" column="parent_id" />
    </resultMap>

    <sql id="Base_Column_List">
        id,menus_index,title,icon,path,parent_id
    </sql>

    <!-- 根据父菜单ID查询子菜单（已废弃，保留兼容性） -->
    <select id="selectbypid" resultMap="BaseResultMap">
        select * from menu where parent_id = #{id}
    </select>

    <!-- 根据角色ID查询该角色有权限的所有菜单（包括一级和二级） -->
    <select id="selectbyroleid" resultMap="BaseResultMap">
        select distinct menu.*
        from menu
        inner join role_menu on menu.menus_index = role_menu.menu_id
        where role_menu.role_id = #{roleid}
        order by menu.menus_index
    </select>
</mapper>
